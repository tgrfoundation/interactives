Update the <base> element in index.html with the path at which the files will be located.

The .htaccess rewrite rules enable users to enter the application through any valid route.  If an .htaccess file cannot be accommodated it can be removed. The application will still function but a user will only be able to enter the application through the base route.